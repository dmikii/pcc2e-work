def greet_user(username): # variable "username" is a PARAMETER: info the function needs to do its job.
	"""Display a simple greeting."""
	print(f"Hello, {username.title()}!")

greet_user('sleeki') # 'sleeki' is an ARGUMENT: info passed from a function call to a function.